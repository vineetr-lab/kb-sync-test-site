User-agent: *
Allow: /

# Overwritten at build time by .github/workflows/pages.yml, which appends the
# Sitemap: line. Kept here so the repo is still coherent if someone serves it
# without the workflow.
Sitemap: https://vineetr-lab.github.io/kb-sync-test-site/sitemap.xml
